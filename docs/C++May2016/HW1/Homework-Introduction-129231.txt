Виждам че имаш задълбочени познания в С++.
Кодът е читаем,  а коментарите са на експертно ниво.

Кодовете са писани на Visual Studio, но не е архивиран целия solution, и които иска да се оправя както може.

В кода е оставена пред-процесорната директива:
#include "stdafx.h"
която не се поддържа от IDE-та, различни от VS.

Това не е проблем, тъй като за C++ VS е само едно възможно решение. Между другото VS предлага съвсем прилична диагностика. Qt Creator e сериозната и крос-платформена алтернатива за C++ проекти.

Кодовете все пак се компилират правилно и работят.

Първата задача:
- решена е с асоциативен масив и това предполага подобрена производителност;
- работи с кирилица. Съкращението UFT, най-вероятно е UTF. Само за информация кодовата таблица не е UTF. С то взи код лесно се вижда че кирилицата е в 8 битов ASCII код:

// To print 8-bit code table
#include <cstdio>
int main()
{
	int i=0;
	for(i=0;i<256;i++)
		printf("%d %c\n",i,i) ;
	return 0;
}

Втората задача:
- правилно работещ код;
- Няма крайна цел свързана с приложимостта на този код, но е подходящ учебен пример за начинаещи програмисти;

И в двете задачи липсва насочващо инфо като readme1st.txt и toDo.txt

Домашното е много добре изпълнено и разбира се получава съответната оценка.

Коментарите, които следват, не са забележки и предполагач че са полезни като теми за размисъл.
